node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Firebase service account (never commit)
secrets/
firebase-admin*.json

# Uploaded images (runtime data, never commit)
uploads/
